<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/calloutContainer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="20dp"
    android:layout_marginBottom="20dp"
    android:orientation="vertical"
    android:background="@drawable/border_blue" >
    
    <FrameLayout
        android:id="@+id/calloutHeader"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@drawable/dot_bg_blue" >
        
	    <TextView
	        android:id="@+id/calloutTitle"
	        style="@style/CalloutTitle" />

    </FrameLayout>
    
	    <TextView android:id="@+id/calloutText"
	        style="@style/CalloutText" />
	    
</LinearLayout>
